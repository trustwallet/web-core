{
  "name": "tw-monorepo",
  "private": true,
  "scripts": {
    "bootstrap": "yarn install && lerna bootstrap",
    "publish": "yarn bootstrap && yarn build && lerna publish -- --access public",
    "build": "lerna run build",
    "nuke": "rm -rf node_modules; for d in packages/*/node_modules; do echo $d; rm -rf $d; done"
  },
  "devDependencies": {
    "lerna": "^3.16.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
