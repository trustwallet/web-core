{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "yarn install && lerna bootstrap",
    "publish": "npm run bootstrap && npm run build && lerna publish -- --access public",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.16.2"
  }
}
