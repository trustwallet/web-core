{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "yarn install && lerna bootstrap",
    "publish": "lerna publish",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.16.2"
  }
}
